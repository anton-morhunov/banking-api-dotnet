<UserControl x:Class="bank_desktop.Views.ClientsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:bank_desktop.Views"
        mc:Ignorable="d"
        d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="300"/>
        </Grid.ColumnDefinitions>
        
        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <TextBlock Grid.Row="0"
                       Text="Clients Information"
                       FontSize="28"
                       FontWeight="Bold"
                       Margin="10"/>
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="100"/>
                    <ColumnDefinition Width="100"/>
                </Grid.ColumnDefinitions>
                
                <Border Grid.Column="0"
                        Margin="10"
                        CornerRadius="25"
                        Background="White"
                        BorderBrush="#E5E7EB"
                        BorderThickness="1">
                    <TextBox 
                             Height="35"
                             Margin="10"
                             BorderThickness="0"
                             Background="Transparent"
                             VerticalContentAlignment="Center"
                             FontSize="16"
                    />
                </Border>
                <Border Grid.Column="1"
                        CornerRadius="25"
                        BorderThickness="1"
                        >
                    <Button 
                            Content="Find"
                            Margin="10"
                            Background="#1a73e8"
                            FontWeight="500"
                            Padding="10px, 18px"
                            BorderThickness="0"
                    />
                </Border>
                <Button Grid.Column="2"
                        Content="Create"
                        Margin="10"
                        Background="#1a73e8"
                        FontWeight="500"
                        Padding="10px, 18px"/>
            </Grid>
            <DataGrid Grid.Row="2"
                      Margin="10"
                      ItemsSource="{Binding Clients}"
                      FontSize="23"
                      AutoGenerateColumns="False">
                <DataGrid.Columns>
                    <DataGridTextColumn
                        Header="Id"
                        Binding="{Binding Id}"
                        />
                    
                    <DataGridTextColumn
                        Header="Name"
                        Binding="{Binding Name}"/>
                    
                    <DataGridTextColumn
                        Header="Email"
                        Binding="{Binding Email}"/>
                    
                    <DataGridTextColumn
                        Header="Phone Number"
                        Binding="{Binding PhoneNumber}"/>
                    
                    <DataGridTextColumn
                        Header="Created Day"
                        Binding="{Binding Created}"/>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
        
        <Border
            Grid.Column="1"
                Background="White"
                Margin="10"
            >
            <StackPanel>
                <TextBlock
                    Text="TheNorthFieldBank"
                    FontSize="24"
                    FontWeight="Bold"
                    Margin="0,0,0,20"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
